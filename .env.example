PORT=3000
DATA_DIR=./data
# Required before adding database connections in production. Use a long random value.
ENCRYPTION_KEY=replace-with-a-32-byte-or-longer-secret
# Set true only for local UI development without Docker access.
DEMO_MODE=false
